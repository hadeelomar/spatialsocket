services:
  server:
    build: .
    ports:
      - "5000:5000"   # websocket
      - "5001:5001"   # http+sse
    cpus: "4"
    mem_limit: 12g
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=dev-secret-change-in-prod
    restart: unless-stopped
